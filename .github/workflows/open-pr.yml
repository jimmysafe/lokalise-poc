name: New Translations
on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
jobs:
  process_translations:
    uses: jimmysafe/lokalise-actions/.github/workflows/translations-open-pr/action.yml@main-v1
    secrets: inherit
